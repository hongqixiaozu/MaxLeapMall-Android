apply plugin: 'com.android.application'
apply plugin: 'com.android.databinding'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.1"

    defaultConfig {
        applicationId "com.maxleap.mall"
        minSdkVersion 14
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            buildConfigField("boolean", "LOG_DEBUG", "false")
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            manifestPlaceholders = [ml_channel: "google_play", ml_region: "cn", ml_appid: APP_ID, ml_apikey: CLIENT_KEY]
        }

        applicationVariants.all { variant ->
            def apk = variant.outputs[0].outputFile;
            def versionName = android.defaultConfig.versionName;
            def newName = apk.name.replace(".apk", "-v" + versionName + ".apk");
            newName = newName.replace("app", "mall");
            variant.outputs[0].outputFile = new File(apk.parentFile, newName);
        }

        debug {
            buildConfigField("boolean", "LOG_DEBUG", "true")
            manifestPlaceholders = [ml_channel: "google_play", ml_region: "cn", ml_appid: APP_ID, ml_apikey: CLIENT_KEY]
        }
    }

    lintOptions {
        abortOnError false
        ignoreWarnings true
    }
}

ext {
    supportLibVersion = "23.1.0"
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile "com.android.support:appcompat-v7:${supportLibVersion}"
    compile "com.android.support:recyclerview-v7:${supportLibVersion}"
    compile "com.android.support:design:${supportLibVersion}"
}
